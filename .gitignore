# OS generated files #
######################
.DS_Store
.DS_Store?
._*
.Spotlight-V100
.Trashes
ehthumbs.db
Thumbs.db
.shopify
node_modules
*.zip
# Credentials — never commit
.env
.env.*

# build output — deployed to Netlify, not served from the theme
dist/

# Netlify CLI local state — carries an absolute local path, not repo content
.netlify/
__pycache__

# scratch probes
.diag.mjs
.shot.mjs
.adpage.mjs
